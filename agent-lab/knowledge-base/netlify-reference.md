# Netlify Reference

Connect the site to a GitHub repository. The production branch is usually main unless the project uses another default branch. The build command and publish directory must match the framework. Environment variables should be configured in Netlify, not committed to GitHub. After deployment, test the live URL before sending it to the client.

## Safe Use
Use these notes as general, non-sensitive reference material. Do not add private data or raw workplace content.
